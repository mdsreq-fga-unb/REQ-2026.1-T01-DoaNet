import 'package:flutter/material.dart';

class TransparenciaPage extends StatelessWidget {
  const TransparenciaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Transparencia'));
  }
}
