import 'package:flutter/material.dart';

class ColaboracaoPage extends StatelessWidget {
  const ColaboracaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Colaboracao'));
  }
}
